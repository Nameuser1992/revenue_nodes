name: Deploy Hugoecho on:echo   push:echo     branches: [main]echo permissions:echo   contents: readecho   pages: writeecho   id-token: writeecho jobs:echo   deploy:echo     runs-on: ubuntu-latestecho     steps:echo       - name: Checkoutecho         uses: actions/checkout@v4echo       - name: Setup Pagesecho         uses: actions/configure-pages@v4echo       - name: Setup Hugoecho         uses: actions/setup-hugo@v3echo         with:echo           extended: trueecho       - name: Buildecho         run: hugo --minifyecho       - name: Upload artifactecho         uses: actions/upload-pages-artifact@v3echo         with:echo           path: ./publicecho       - name: Deployecho         uses: actions/deploy-pages@v4
